<odoo>

    <!-- Form -->
    <record id="view_hr_arl_entity_form" model="ir.ui.view">
        <field name="name">hr.arl.entity.form</field>
        <field name="model">hr.arl.entity</field>
        <field name="arch" type="xml">
            <form>
                <group string="Configurar ARL">
                    <field name="name" />
                    <field name="nit" />
                    <field name="code" />
                    <field name="phone" />
                    <field name="address" />
                    <field name="arl_account" />
                    <field name="active" />
                </group>
            </form>
        </field>
    </record>

    <!-- List -->
    <record id="view_hr_arl_entity_list" model="ir.ui.view">
        <field name="name">hr.arl.entity.list</field>
        <field name="model">hr.arl.entity</field>
        <field name="arch" type="xml">
            <list>
                <field name="name" />
                <field name="nit" />
                <field name="code" />
                <field name="phone" />
                <field name="address" />
                <field name="arl_account" />
                <field name="active" />
            </list>
        </field>
    </record>

    <!-- Action -->
    <record id="action_hr_arl_entity" model="ir.actions.act_window">
        <field name="name">ARL</field>
        <field name="res_model">hr.arl.entity</field>
        <field name="view_mode">list,form</field>
    </record>

    <!-- Menu -->
    <menuitem id="menu_hr_arl_entity" name="Configurar ARL"
        parent="payroll.contributions" action="action_hr_arl_entity" />
</odoo>