<odoo>

    <!-- Action -->
    <record id="action_hr_service_bonus" model="ir.actions.act_window">
        <field name="name">Liquidación</field>
        <field name="res_model">hr.service.bonus</field>
        <field name="view_mode">list,form</field>
    </record>

    <!-- Form -->
    <record id="view_hr_service_bonus_form" model="ir.ui.view">
        <field name="name">hr.service.bonus.form</field>
        <field name="model">hr.service.bonus</field>
        <field name="arch" type="xml">
            <form string="Liquidación de Nómina">
                <header>
                    <button name="action_generate_settlement_lines" type="object"
                        string="Generar Liquidación" class="btn-primary oe_highlight" />
                </header>
                <sheet>
                    <group>
                        <group>
                            <field name='bonus_date_end' />
                            <field name="contract_start_date" />
                        </group>
                        <group>
                            <field name="days_period" />
                            <field name="absences" />
                            <field name="days_settlement" />
                        </group>
                    </group>

                    <notebook>
                        <page string="Líneas de Liquidación">
                            <field name="line_ids">
                                <list editable="bottom">

                                    <field name="start_date" />
                                    <field name="end_date" />
                                    <field name="days_period" />
                                    <field name="absences" />
                                    <field name="days_settlement" />
                                    <field name="average_wage" />
                                    <field name="value_wage" />
                                    <field name="advances" />
                                    <field name="net_value" />
                                    <field name="currency_id" />
                                </list>
                                <form>
                                    <group>
                                        <field name="concept" />
                                        <field name="start_date" />
                                        <field name="end_date" />
                                        <field name="days_period" />
                                        <field name="absences" />
                                        <field name="days_settlement" />
                                        <field name="average_wage" />
                                        <field name="value_wage" />
                                        <field name="advances" />
                                        <field name="net_value" />
                                        <field name="currency_id" />
                                    </group>
                                </form>
                            </field>
                        </page>
                        <page string="Totales">
                            <group>
                                <!-- Información adicional o resumen -->
                            </group>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- List -->
    <record id="view_hr_service_bonus_list" model="ir.ui.view">
        <field name="name">hr.service.bonus.list</field>
        <field name="model">hr.service.bonus</field>
        <field name="arch" type="xml">
            <list>
                <field name='state' />
                <field name="name" />   
                <field name="employee_id" />
                <field name='termination_reason' />
                <field name="creation_date" />
                <field name="cutoff_date" />
            </list>
        </field>
    </record>

    <!-- Menu Item -->
    <menuitem name="Liquidación" id="service.bonus" parent="payroll.payroll"
        action='action_hr_service_bonus' sequence='10' />

</odoo>